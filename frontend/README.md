# Recos Frontend

Port del prototipo "academiascraper-ai" como SPA React/Vite.

## Scripts
- `npm install`
- `npm run dev`
- `npm run build`

## Build destino
El build se emite a `../backend/static/frontend` para que Django sirva los assets.
